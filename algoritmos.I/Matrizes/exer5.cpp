/*5. Faça um algoritmo que leia duas matrizes de inteiros AnXm e BpXq (2 ≤ n,m,p,q ≤ 10), e calcule a
matriz resultante da multiplicação de AxB. Caso não seja possível realizar a multiplicação deve-se
dar uma mensagem de erro. */

#include <iostream>
using namespace std;

int main(){

int n, m, p, q;

do{

    cout << "informe quantas linhas da matriz A deseja: " << endl;
    cin >>



}while(n < 2 || n > 10 || m < 2 || m > 10 || p < 2 || p > 10 || q < 2 || q > 10);







    return 0;
}